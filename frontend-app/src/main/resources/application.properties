spring.application.name=frontend-app
server.port=9090

# Gateway is the single entry for all API calls (auth, courses, trainees, etc.)
app.gateway-url=http://localhost:8080
app.api-prefix=${app.gateway-url}/api
